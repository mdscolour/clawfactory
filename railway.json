{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "NIXPACKS",
    "noCache": true
  },
  "deploy": {
    "rootDirectory": "",
    "startCommand": "cd backend && npm start"
  }
}
